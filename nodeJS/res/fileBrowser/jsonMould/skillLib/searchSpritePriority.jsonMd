<root>
	<unit id = "lib_none">			
		<!--同权高优先级材质(逗号分开)-->
		<material>demo0,demo1</material>
		<!--优先值 = 距离差*距离比例值+ 角度差*角度比例值
			[说明]根据算法生成 优先值，该值越小，选取几率越大.
		-->
		<!--距离比例值(大于等于0的浮点数).-->
		<dis_rate>10</dis_rate>
		<!--角度比例值(大于等于0的浮点数)-->
		<angle_rate>1</angle_rate>
	</unit>
</root>